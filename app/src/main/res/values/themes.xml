<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.CodeChallengeTM" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <!-- BottomSheet theme-->
        <item name="bottomSheetDialogTheme">@style/ThemeOverlay.BottomSheetDialog</item>

        <!-- enable window content transitions -->
<!--        <item name="android:windowActivityTransitions">true</item>-->

        <!-- specify enter and exit transitions -->
<!--        <item name="android:windowEnterTransition">@transition/change_image_transform</item>-->
<!--        <item name="android:windowExitTransition">@transition/change_image_transform</item>-->

        <!-- specify shared element transitions -->
<!--        <item name="android:windowSharedElementEnterTransition">-->
<!--            @transition/change_image_transform</item>-->
<!--        <item name="android:windowSharedElementExitTransition">-->
<!--            @transition/change_image_transform</item>-->

    </style>
    <style name="SplashTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="android:windowBackground">@drawable/ic_spash</item>
        <item name="colorPrimary">@color/black</item>
    </style>


    <style name="ThemeOverlay.BottomSheetDialog" parent="@style/ThemeOverlay.MaterialComponents.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/Widget.BottomSheet.RoundCorner</item>
    </style>

    <style name="Widget.BottomSheet.RoundCorner" parent="Widget.MaterialComponents.BottomSheet">
        <item name="shapeAppearanceOverlay">@style/ShapeAppearanceOverlay.BottomSheet</item>
    </style>

    <style name="ShapeAppearanceOverlay.BottomSheet" parent="">
        <item name="cornerSizeTopLeft">@dimen/dp_10</item>
        <item name="cornerSizeTopRight">@dimen/dp_10</item>
        <item name="cornerFamily">rounded</item>
    </style>
</resources>